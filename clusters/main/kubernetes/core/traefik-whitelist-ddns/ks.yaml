apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-whitelist-ddns
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/core/traefik-whitelist-ddns/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

