apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: filezilla
  namespace: mc-jan
spec:
  interval: 15m
  chart:
    spec:
      chart: minecraft-java
      version: 11.2.3
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  releaseName: filezilla
  values:
    persistence:
      config:
        enabled: true
        mountPath: "/config"
      varrun:
        enabled: false
      mc:
        enabled: true
        type: pvc
        existingClaim: "pvc-0e3afef4-7c78-49da-9239-aa819af8e243"
        mountPath: /mc
        targetSelectAll: true
