apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: capacitor
  namespace: flux-system
  chart:
    spec:
      chart: capacitor
      version: 0.4.8
      sourceRef:
        kind: OCIRepository
        name: capacitor
        namespace: flux-system
  spec:
    releaseName: capacitor
