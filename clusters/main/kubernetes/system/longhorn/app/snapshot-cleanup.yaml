apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  namespace: longhorn-system
  name: snapshot-cleanup
spec:
  name: "Snapshot Cleanup"
  cron: 0 10 * * *
  task: snapshot-cleanup
  concurrency: 1
  groups:
    - default
