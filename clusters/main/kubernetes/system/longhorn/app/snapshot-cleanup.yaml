apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  namespace: longhorn-system
spec:
  name: "Snapshot Cleanup"
  cron: 0 10 * * *
  task: snapshot-cleanup
  concurrency: 1
  groups:
    - default
