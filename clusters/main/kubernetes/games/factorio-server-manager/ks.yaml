apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: factorio-server-manager
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/games/factorio-server-manager/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

